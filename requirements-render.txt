# =============================================================================
# 🚀 RENDER DEPLOYMENT - requirements.txt
# =============================================================================
# These are the Python packages needed for the admin dashboard
# Render will automatically install these when deploying

requests>=2.28.0
Flask>=2.3.0
gunicorn>=21.0.0
